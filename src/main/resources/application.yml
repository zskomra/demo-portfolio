spring:
  application:
    name: portfolio-demo
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
logging:
  level:
    root: info
    sql: debug
    projects.portfoliodemo: debug

